#ifndef SPIC_GAME_FOLDERFILE_HPP
#define SPIC_GAME_FOLDERFILE_HPP

#include <string>

namespace game {
    struct FolderFile {
        std::string Name;
        std::string File;
    };
}

#endif //SPIC_GAME_FOLDERFILE_HPP
